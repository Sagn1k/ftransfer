# user files being shared — never commit these
shared/*
!shared/.gitkeep

# build output
clients/mac/FTransfer.app/
clients/mac/build/
clients/mac/vendor/
dist/

# local tooling & OS noise
.claude/
.DS_Store
*.log
