# ftransfer — prebuilt app

This branch holds only the prebuilt `FTransfer.app` zip, so it can be fetched
from `raw.githubusercontent.com` (GitHub *release* asset hosting is blocked on
some corporate networks).

Install: see [docs/INSTALL.md on the main branch](https://github.com/Sagn1k/ftransfer/blob/main/docs/INSTALL.md).
